{
  "name": "agent-sandbox-pi-agent-packages",
  "version": "1.1.0",
  "private": true,
  "description": "Pi packages for the seeded agent home. Regenerate package-lock.json with `npm install --package-lock-only --ignore-scripts`.",
  "dependencies": {
    "pi-claude-bridge": "0.7.0"
  }
}
