{
  "name": "agent-sandbox-toolchain",
  "version": "1.1.0",
  "private": true,
  "description": "Pinned Node tools for the agent-sandbox image. Regenerate package-lock.json with `npm install --package-lock-only --ignore-scripts`.",
  "dependencies": {
    "@anthropic-ai/claude-code": "2.1.263",
    "@earendil-works/pi-coding-agent": "0.85.1"
  }
}
